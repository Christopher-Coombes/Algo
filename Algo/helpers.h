#pragma once
#include <memory>
#include <iostream>

int max(int a, int b);

int int_less(int a, int b);

int numwidth(int n);

double rand0to1();
int rand0toN(int n);

// bool find (T x);         // true if found
// bool insert (T x);       // true if exists already
// bool remove (T x);       // true if exists (and removed)
// bool clear ();           // true if there was something to clear
// void print (std::ostream& stream);
// 
// 
// 
//